{
  "buildCommand": "npm run build",
  "installCommand": "npm install --include=optional",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

